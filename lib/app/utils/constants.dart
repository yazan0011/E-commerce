import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.brown;
  static const background = Color(0xFFF5F5F5); // Soft white
  static const grey = Colors.grey;
}

class AppTextStyles {
  static const heading =
      TextStyle(fontSize: 24, fontWeight: FontWeight.bold, color: Colors.black);
  static const subHeading = TextStyle(fontSize: 16, color: Colors.grey);
  static const button =
      TextStyle(fontSize: 18, fontWeight: FontWeight.bold, color: Colors.white);
}
